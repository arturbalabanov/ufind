div(ng-include='\'partials/navbar\'')

h3
  | This is the profile of {{ user.name }}
  small  @{{ user.username }}
  |  
  a.btn.btn-xs.btn-success(ng-click='follow()' ng-show='!following')
    span.glyphicon.glyphicon-plus
    |  Follow
  a.btn.btn-xs.btn-danger(ng-click='unfollow()' ng-show='following')
    span.glyphicon.glyphicon-minus
    |  Unfollow

div(ng-include='\'partials/tweets-list\'')
